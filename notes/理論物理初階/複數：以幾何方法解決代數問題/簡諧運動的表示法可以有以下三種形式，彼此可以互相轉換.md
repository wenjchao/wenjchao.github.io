# 簡諧運動的表示法可以有以下三種形式，彼此可以互相轉換

## 內文
1. $x(t) = ze^{i\omega t}$ 的實部，其中 z 為複數，為複振幅
2. $x(t) = re^{i(\omega t + \theta)}$ 的實部，其中 r 是實數（實振幅），$\theta$ 是相位
3. $x(t) = a \cos(\omega t) + b\sin(\omega t)$

其中 $z =  re^{i\theta} = a - bi$
